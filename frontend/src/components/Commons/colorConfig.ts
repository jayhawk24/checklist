import { primaryColors } from "../../../tailwind.config";

export const cardColorConfig = {
    backgroundColor: primaryColors[600],
    edgeColors: {
        bottom: primaryColors[800],
        right: primaryColors[900]
    }
};
